language: ruby
rvm:
  - 2.4.0
branches:
  only:
  - custom
script: bundle exec jekyll build --config ./_config.yml
deploy:
  provider: pages
  github_token: $GITHUB_TOKEN
  local_dir: _site
  on:
    branch: custom
    target_branch: master
